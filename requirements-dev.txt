-r requirements.txt
pytest==9.1.1
coverage==7.16.1

# What tests/test_architecture.py checks the import graph with (ADR-0047). It
# has no dependencies of its own -- it parses the package with `ast` -- and
# nothing in buy_agent/ imports it, so it stays out of requirements.txt and out
# of the image.
archunitpython==1.7.0

# The per-test cap `pytest.ini` sets. pytest has no timeout of its own, so a test
# that stops -- a patch spelt wrong and a real fetch behind it, a socket nothing
# will answer -- hangs the run rather than failing it, which on a runner is six
# hours of nothing and locally is a process to go and find. Test-only, like the
# two above: nothing in buy_agent/ imports it, so it stays out of the image.
pytest-timeout==2.4.0

# The linter `.github/workflows/ci.yml` runs over `buy_agent/` after the tests,
# configured by `.pylintrc` and recorded in ADR-0048. Test-only like the three
# above -- nothing in `buy_agent/` imports it, so it stays out of the image -- and
# it reads the package rather than running it, so a lint costs no model, no
# network and no run.
pylint==4.0.9

# The type checker `.github/workflows/ci.yml` runs over `buy_agent/` beside pylint,
# configured in `setup.cfg` and recorded in ADR-0063. The package was annotated
# throughout and nothing read the annotations, which left them prose that happens to
# parse. Test-only like the four above -- nothing in `buy_agent/` imports it, so it stays
# out of the image -- and, like the linter, it reads the package rather than running it.
mypy==2.3.1
