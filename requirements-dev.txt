-r requirements.txt
pytest==9.1.1
coverage==7.16.0

# What tests/test_architecture.py checks the import graph with (ADR-0047). It
# has no dependencies of its own -- it parses the package with `ast` -- and
# nothing in buy_agent/ imports it, so it stays out of requirements.txt and out
# of the image.
archunitpython==1.5.0
