# Direct dependencies, pinned to the versions this was tested against.
# Transitive packages (httpx, ollama, openai, primp, ...) are resolved by pip.
langchain-core==1.6.0
# One per model server: `buy_agent.providers` builds a ChatOllama or a ChatOpenAI
# pointed at vLLM's OpenAI-compatible API, and imports both at module level, so
# neither is optional (ADR-0028).
langchain-ollama==1.1.0
langchain-openai==1.1.0
ddgs==9.15.0
pydantic==2.13.4
httpx==0.28.1
lxml==6.1.2
